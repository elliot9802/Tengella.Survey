@model IEnumerable<SurveyForm>

@{
    ViewData["Title"] = "Surveys";
}

<div class="container">
    <div class="row">
        <div class="col-12 col-md-6 col-lg-4 mb-4 d-flex align-items-stretch">
            <div class="card survey-card w-100">
                <div class="card-body d-flex flex-column align-items-center justify-content-center">
                    <i class="fas fa-plus-circle fa-3x mb-3"></i>
                    <h5 class="card-title">Create New Survey</h5>
                </div>
                <a href="@Url.Action("CreateSurvey", "Survey")" title="Create Survey" class="stretched-link"></a>
            </div>
        </div>
        <div class="col-12 col-md-6 col-lg-4 mb-4 d-flex align-items-stretch">
            <div class="card survey-card w-100">
                <div class="card-body d-flex flex-column align-items-center justify-content-center">
                    <i class="fas fa-chart-bar fa-3x mb-3"></i>
                    <h5 class="card-title">Survey Summary</h5>
                </div>
                <a href="@Url.Action("SurveySummary", "SurveyAnalysis")" title="View Summary" class="stretched-link"></a>
            </div>
        </div>
        <div class="col-12 col-md-6 col-lg-4 mb-4 d-flex align-items-stretch">
            <div class="card survey-card w-100">
                <div class="card-body d-flex flex-column align-items-center justify-content-center">
                    <i class="fas fa-address-book fa-3x mb-3"></i>
                    <h5 class="card-title">Distribution List</h5>
                </div>
                <a href="@Url.Action("DistributionList", "Distribution")" title="View Distribution List" class="stretched-link"></a>
            </div>
        </div>

        @foreach (var survey in Model)
        {
            <div class="col-12 col-md-6 col-lg-4 mb-4 d-flex align-items-stretch">
                <div class="card survey-card w-100">
                    @if (survey.ClosingDate < DateTime.Today)
                    {
                        <div class="badge badge-danger" style="top: 10px; right: 10px;">Closed</div>
                    }
                    <div class="card-body">
                        <h5 class="card-title">@survey.Name</h5>
                        <p class="card-text">@survey.Type</p>
                        <p class="card-text">Survey closes: @survey.ClosingDate.ToShortDateString()</p>
                        <p class="card-text">@survey.Questions.Count() @(survey.Questions.Count() == 1 ? "question" : "questions")</p>
                        <ul>
                            @foreach (var question in survey.Questions)
                            {
                                <li>
                                    @question.Text
                                    @(question.Options.Count() > 0 ? $" ({question.Options.Count()} options)" : "")
                                </li>
                            }
                        </ul>
                    </div>
                    <div class="card-footer d-flex justify-content-around">
                        <a href="@Url.Action("RespondSurvey", "Response", new { id = survey.SurveyFormId })" title="Take Survey" class="btn btn-link"><i class="fas fa-arrow-right"></i></a>
                        <a href="@Url.Action("PreviewSurvey", "Survey", new { id = survey.SurveyFormId })" title="Preview Survey" class="btn btn-link"><i class="fas fa-eye"></i></a>
                        <a href="@Url.Action("CreateSurvey", "Survey", new { id = survey.SurveyFormId })" title="Edit Survey" class="btn btn-link"><i class="fas fa-edit"></i></a>
                        <a href="@Url.Action("DeleteSurvey", "Survey", new { id = survey.SurveyFormId })" title="Delete Survey" class="btn btn-link"><i class="fas fa-trash-alt"></i></a>
                        <a href="@Url.Action("CopySurvey", "Survey", new { id = survey.SurveyFormId })" title="Copy Survey" class="btn btn-link"><i class="fas fa-copy"></i></a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
